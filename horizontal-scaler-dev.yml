logging:
  level:
     com.cooksys: debug

eventBus:
  broker: redis
  redisHost: redis
  redisPort: 6379
  redisTopic: csrt-event-bus

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/